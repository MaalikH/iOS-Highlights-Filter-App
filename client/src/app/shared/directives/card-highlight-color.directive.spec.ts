import { CardHighlightColorDirective } from './card-highlight-color.directive';

describe('CardHighlightColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHighlightColorDirective();
    expect(directive).toBeTruthy();
  });
});
