import { ChipHighlightColorDirective } from './chip-highlight-color.directive';

describe('ChipHighlightColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipHighlightColorDirective();
    expect(directive).toBeTruthy();
  });
});
